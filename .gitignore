# Local-only; not needed in the GitHub Pages repo
.venv/
__pycache__/
*.pyc
history/
weekly_update.log

# Warehouse build artifacts (rebuildable from raw_snapshots/)
nwsl_dw.duckdb
nwsl_dw.duckdb.wal
raw_snapshots/

# dbt build artifacts
dbt/target/
dbt/logs/
dbt/.user.yml

# dbt lives in its own venv (setup_dbt.sh)
.venv-dbt/
dbt_setup.log
warehouse_run.log

# Local build artifacts and scratch -- index.html is the published copy
dashboard.html
_mock_dashboard.html
probe_report.txt
_to_delete/

# freeze-frame study: the StatsBomb clone (~380MB) and derived shot tables
freeze_frames/statsbomb-open-data/
freeze_frames/shots_2023*.csv

# Tableau writes a lock/recovery file beside the workbook while it is open
tableau/.~*.twbr
# R session leftovers from running/deploying the Shiny app in place
.Rhistory
.RData
.Rproj.user/
